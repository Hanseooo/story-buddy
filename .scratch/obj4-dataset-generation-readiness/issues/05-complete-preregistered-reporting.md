# 05 — Complete preregistered reporting and claim assignment

**What to build:** The Objective 4 report contains every preregistered measurement and assigns the conclusion exactly from the frozen claim ladder. A reader can audit overall and human/non-human performance without opening raw held-out records.

**Blocked by:** 04 — Make evaluation locking and held-out resume fail closed.

**Status:** completed
**Labels:** `criticality:critical`, `complexity:high`

- [x] Each registered judge reports sample count, precision, recall, F1 with interval, AUROC, calibration, latency, parse-failure count and rate, label prevalence, and prediction rate.
- [x] Human and non-human slices report the registered performance, judge-versus-human agreement, and inter-rater agreement without leaking story or asset identifiers.
- [x] Prediction-rate drift and every other registered comparison are computed from immutable prediction evidence.
- [x] Claim Rung A uses only its registered comparisons; Rung B adds the registered recall non-regression condition; Rung C records the Objective 4 requirement as met while keeping the deployment decision distinct; only Rung D marks the research requirement failed.
- [x] Golden deterministic tests cover all claim-ladder rungs, missing/undefined metric behavior, both character slices, parse failures, and latency aggregation.
- [x] The emitted report validates against one documented schema and can be regenerated byte-for-byte from the same immutable inputs.
- [x] Evaluation specs, preregistration interpretation, and canonical operator instructions agree with the implemented fields and conclusions.


## Verification (2026-08-28)

Criteria confirmed against committed code, not self-reported. Implementing commit: `f0d8f5e`
"feat(obj4): complete preregistered reporting" (2026-08-25, 6 files, +764/-109).

| Criterion | Evidence |
|---|---|
| Full preregistered metric set emitted | `evaluate.py:128-139` — `f1`, `f1_ci95`, `auroc`, `calibration`, `latency_ms`, `cold_start_latency_ms`, `parse_failures`, `label_prevalence`, `prediction_rate` |
| Both human and non-human slices plus agreement required | `evaluate.py:206-222` |
| Claim-ladder rung assigned from frozen criteria | `classify_claim_rung`, `evaluate.py:1220-1238` |
| Objective-met and ship-candidate kept distinct | `objective4_requirement_met = rung != "D"`, `ship_candidate = rung in ("A","B")`; invariant enforced at `evaluate.py:228-231` |
| Report regenerates byte-for-byte | `evaluate.py:1523` `json.dumps(report, indent=2, sort_keys=True)` |
| Schema documented | `docs/specs/judge-finetune.md:692` (§7.7), referenced from the runbook |
| Golden tests | `test_finetune_evaluate.py:531, 633, 649, 724, 889` |

The boxes were previously unticked while the implementation was complete; this reconciles the
record. No code changed for this ticket.
